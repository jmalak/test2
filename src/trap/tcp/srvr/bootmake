#pmake: boot serv remote tcp

bootstrap = 1

os    = $(host_os)
srv   = tcp
cpu   = x86
which = SERVER
type  = remote_link

extra_objs = clibext.obj

!define substitute_linker

!include $(trap_dir)/master.mif

$(target) : $(libs) $(objs) $(__MAKEFILES__)
    $(cl) $(clflags) $(objs) $(cl_libs)
